# 轨迹功能实图验证（2026-09-21）

使用 [瀛洲园扩展路线](../examples/yingzhouyuan/README.md)，从原始 Gaussian 文件读取13,860,573点；透明度阈值后12,029,312点，轨迹裁切后448,436点，两个后端都全量渲染裁切结果。轨迹465行，长度约12.699场景单位；共线合并后裁切使用36段，标注仍使用原轨迹。

| 项目 | 点云 | Gaussian |
|---|---|---|
| 输出 | `outputs/trajectory_validation/final_extended_points.png` | `outputs/trajectory_validation/final_extended_gaussian.png` |
| 最终尺寸 / SSAA | 1800×1400 / 2 | 1800×1400 / 2 |
| 渲染点数 | 448,436 | 448,436 |
| 报告内流程耗时 | 8.20秒 | 10.00秒 |

耗时是单次本机记录，不是性能基准，且不包含最终报告写入中的全部耗时。每张图旁有完整 JSON、全属性裁切 PLY 和规范化轨迹 CSV。结果目录被 Git 忽略，大文件不入库。

已程序核对：配置、各阶段点数、相机视图矩阵、内参、视锥尖端和裁切索引摘要完全一致；裁切 PLY 字节哈希也一致：

```text
b6abe4c973b47b37055539b375c843a649495e0a4e5dbe1cdef225316387823f
```

已目视检查两张图片：起终点完整，红线和视锥清晰，门口台阶、转弯、楼梯上升段和平台在取景内。点云保留离散颗粒；Gaussian 表面较连续，边缘有原模型的拖影。高处仍有一小块悬浮结构，自动距离/高度裁切不具备构件语义，未为了消除它加入场景专用删点逻辑。

运行环境为 Python3.11.14、NumPy1.26.0、SciPy1.15.3、Pillow11.3.0；GPU 使用 PyTorch2.7.0+cu128、gsplat1.5.3。此次 GPU 验证复用了本机旧工程已安装的 gsplat 与已编译 CUDA 扩展，通过外部启动环境加载，先导入 torch，再将编译扩展注册到 `gsplat.csrc`。仓库代码没有写入这些机器路径；这证明实际 GPU 渲染可运行，**不代表全新环境的安装/编译流程已验证**。

CPU 单元测试21项通过，覆盖合成的不同几何与异常输入，不依赖瀛洲园资产。此次没有执行八场景完整回归、跨平台验收或显存峰值测量。实图生成时，代码整理尚为工作区变更；这些历史报告中的 Git 提交号需要结合其中记录的未提交状态理解。
